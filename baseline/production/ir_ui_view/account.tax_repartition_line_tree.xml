<list editable="bottom" create="1" delete="1">
                    <field name="sequence" widget="handle"/>
                    <field name="factor_percent" invisible="repartition_type == 'base'" widget="account_tax_repartition_line_factor_percent"/>
                    <field name="repartition_type"/>
                    <field name="account_id" invisible="repartition_type == 'base'" options="{'no_create': True}"/>
                    <field name="tag_ids" widget="many2many_tags" options="{'no_create': True}" domain="tag_ids_domain"/>
                    <field name="use_in_tax_closing" optional="hidden" invisible="repartition_type == 'base'"/>
                    <field name="company_id" column_invisible="True"/>
                    <field name="tag_ids_domain" column_invisible="True"/>
                </list>