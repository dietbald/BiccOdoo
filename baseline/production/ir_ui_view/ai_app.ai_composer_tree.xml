<list string="Default Prompts">
                <field name="name" readonly="1" string="Name"/>
                <field name="interface_key"/>
                <field name="focused_models" widget="many2many_tags" optional="hide"/>
                <field name="ai_agent" widget="many2one_avatar"/>
            </list>