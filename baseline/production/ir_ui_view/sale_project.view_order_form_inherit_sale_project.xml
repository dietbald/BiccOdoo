<data><button name="action_view_invoice" position="before">
                <button name="action_view_project_ids" type="object" class="oe_stat_button" icon="fa-puzzle-piece" invisible="not show_project_button" groups="project.group_project_user">
                    <div class="o_stat_info">
                        <div class="o_row">
                            <span class="o_stat_value order-first">
                                <field name="project_count"/>
                            </span>
                            <span class="o_stat_text"> Projects</span>
                        </div>
                        <div class="o_row">
                            <span class="o_stat_value order-first">
                                <field name="tasks_count"/>
                            </span>
                            <span class="o_stat_text"> Tasks</span>
                        </div>
                    </div>
                </button>
                <button name="action_view_milestone" type="object" class="oe_stat_button" icon="fa-check-square-o" invisible="not is_product_milestone or not project_ids or state == 'draft'" groups="project.group_project_milestone">
                    <field name="milestone_count" widget="statinfo" string="Milestones"/>
                </button>
            </button>
            <field name="journal_id" position="before">
                <field name="project_id" groups="project.group_project_user" context="{'default_allow_billable': True, 'default_partner_id': partner_id, 'order_id': id, 'order_state' : state}"/>
            </field>
        </data>