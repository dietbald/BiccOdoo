<gantt date_start="planned_date_begin" date_stop="date_deadline" default_scale="month" scales="day,week,month,quarter,year" color="project_id" string="Planning" js_class="task_sharing_gantt" precision="{'day': 'hour:quarter', 'week': 'day:half', 'month': 'day:half'}" default_group_by="stage_id" pill_label="True" total_row="True" cell_create="False" dependency_field="depend_on_ids" dependency_inverted_field="dependent_ids">
                <templates>
                    <div t-name="gantt-popover">
                        <footer replace="0">
                           <button name="action_unschedule_task" type="object" string="Unschedule" class="btn btn-sm btn-secondary"/>
                        </footer>
                    </div>
                </templates>
                <field name="project_id"/>
                <field name="allow_milestones"/>
                <field name="milestone_id"/>
                <field name="portal_user_names"/>
                <field name="partner_id"/>
                <field name="dependent_tasks_count"/>
            </gantt>