<form class="o_form_project_tasks">
                    <group>
                        <field name="display_name" string="Task Title" placeholder="e.g. Send Invitations" required="1"/>
                        <field name="project_id" widget="project" invisible="context.get('default_project_id', False)" placeholder="Private" class="o_project_task_project_field" domain="[('type_ids', 'in', context.get('default_stage_id'))] if context.get('default_stage_id') else []" required="is_template"/>
                        <field name="user_ids" options="{'no_open': True}" widget="many2many_avatar_user"/>
                        <field name="company_id" invisible="1"/>
                        <field name="parent_id" invisible="1" groups="base.group_no_one"/>
                        <field name="description" invisible="1"/>
                    </group>
                </form>