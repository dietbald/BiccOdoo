<form string="Survey" class="o_survey_form">
                <field name="id" invisible="1"/>
                <field name="session_state" invisible="1"/>
                <field name="question_ids" invisible="1"/>
                <field name="session_available" invisible="1"/>
                <header>
                    <button name="action_send_survey" string="Share" type="object" class="oe_highlight" invisible="not active"/>
                    <button name="action_result_survey" string="See results" type="object" class="oe_highlight" invisible="answer_done_count &lt;= 0"/>
                    <button name="action_start_session" string="Create Live Session" type="object" class="d-none d-sm-block" invisible="session_state or not active or not session_available"/>
                    <button name="action_open_session_manager" string="Open Session Manager" type="object" class="d-none d-sm-block" invisible="not session_state"/>
                    <button name="action_end_session" string="Close Live Session" type="object" class="d-none d-sm-block" invisible="session_state not in ['ready', 'in_progress']"/>
                    <button name="action_test_survey" string="Test" type="object" invisible="not active or not question_ids"/>
                    <button name="action_unarchive" string="Reopen" class="btn-primary" type="object" invisible="active"/>
                    <button name="action_archive" string="Close" type="object" invisible="not active"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_survey_user_input" type="object" class="oe_stat_button" invisible="access_mode == 'public'" icon="fa-envelope-o">
                            <field string="Registered" name="answer_count" widget="statinfo"/>
                        </button>
                        <button name="action_survey_user_input_certified" type="object" class="oe_stat_button" invisible="not certification" icon="fa-trophy">
                            <field string="Certified" name="success_count" widget="statinfo"/>
                        </button>
                        <button name="action_survey_user_input_completed" type="object" class="oe_stat_button" icon="fa-check-square-o">
                            <field string="Participants" name="answer_done_count" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="background_image" widget="image" class="oe_avatar"/>
                    <field name="allowed_survey_types" invisible="True"/>
                    <field name="survey_type" widget="radio_selection_with_filter" options="{'horizontal': True, 'allowed_selection_field': 'allowed_survey_types'}" class="o_field_radio" invisible="not allowed_survey_types"/>
                    <div class="oe_title" style="width: 100%;">
                        <h1>
                            <field name="title" options="{'line_breaks': False}" widget="text" placeholder="e.g. Satisfaction Survey"/>
                        </h1>
                    </div>
                    <group>
                        <group class="ps-md-0">
                            <field name="user_id" domain="[('share', '=', False)]" widget="many2one_avatar_user"/>
                            <field name="active" invisible="1"/>
                            <field name="has_conditional_questions" invisible="1"/>
                            <field name="lang_ids" widget="many2many_tags" placeholder="All installed languages" options="{'no_quick_create': True, 'no_create_edit': True, 'no_open': True}"/>
                        </group>
                        <group class="ps-3 ps-md-0 ps-lg-3">
                            <field name="restrict_user_ids" widget="many2many_tags_avatar"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Questions" name="questions">
                            <field name="question_and_page_ids" nolabel="1" widget="question_page_one2many" mode="list,kanban" context="{'default_survey_id': id}">
                                <list decoration-bf="is_page">
                                    <field name="sequence" widget="handle"/>
                                    <field name="title" widget="survey_description_page"/>
                                    <field name="background_image" column_invisible="True"/>
                                    <field name="question_type"/>
                                    <field name="is_time_limited" string="Time Limit" optional="hide" column_invisible="not parent.session_available"/>
                                    <field name="time_limit" optional="hide" invisible="not is_time_limited" column_invisible="not parent.session_available"/>
                                    <field name="is_time_customized" column_invisible="True"/>
                                    <field name="constr_mandatory" optional="hide"/>
                                    <field name="is_page" column_invisible="True"/>
                                    <field name="questions_selection" column_invisible="True"/>
                                    <field name="survey_id" column_invisible="True"/>
                                    <field name="random_questions_count" column_invisible="parent.questions_selection == 'all'" invisible="not is_page"/>
                                    <field name="triggering_question_ids" column_invisible="True"/>
                                    <field name="triggering_answer_ids" column_invisible="True" widget="many2many_tags"/> <!-- widget to fetch display_name -->
                                    <widget name="survey_question_trigger" width="30px"/>
                                    <button name="copy" type="object" icon="fa-clone" title="Duplicate Question"/>
                                    <control>
                                        <create name="add_question_control" string="Add a question"/>
                                        <create name="add_section_control" string="Add a section" context="{'default_is_page': True, 'default_questions_selection': 'all'}"/>
                                    </control>
                                </list>
                            </field>
                        </page>
                        <page string="Options" name="options">
                            <group name="options">
                                <group string="Questions" name="questions">
                                    <field name="questions_layout" widget="radio" force_save="1" readonly="session_state in ['ready', 'in_progress'] or survey_type == 'live_session'"/>
                                    <field name="progression_mode" widget="radio" invisible="questions_layout == 'one_page' or survey_type == 'live_session'"/>
                                    <field name="questions_selection" widget="radio" invisible="survey_type == 'live_session'"/>
                                    <field name="users_can_go_back" string="Allow Roaming" invisible="questions_layout == 'one_page' or survey_type == 'live_session'"/>
                                </group>
                                <group string="Participants" name="participants">
                                    <field name="access_mode" force_save="1" readonly="survey_type == 'live_session'"/>
                                    <field name="users_login_required"/>
                                    <label for="is_attempts_limited" string="Limit Attempts" invisible="survey_type == 'live_session' or access_mode == 'public' and not users_login_required"/>
                                    <div class="o_checkbox_optional_field" invisible="survey_type == 'live_session' or access_mode == 'public' and not users_login_required">
                                        <field name="is_attempts_limited" nolabel="1"/>
                                        <div invisible="not is_attempts_limited">
                                            to <field name="attempts_limit" nolabel="1" class="oe_inline"/> attempts
                                        </div>
                                    </div>
                                </group>
                                <group string="Time &amp; Scoring" name="scoring" invisible="survey_type == 'survey'">
                                    <!-- Time -->
                                    <label for="is_time_limited" string="Survey Time Limit" invisible="survey_type == 'live_session'"/>
                                    <div class="o_checkbox_optional_field" name="is_time_limited" invisible="survey_type == 'live_session'">
                                        <field name="is_time_limited" nolabel="1"/>
                                        <div invisible="not is_time_limited">
                                            <field name="time_limit" widget="float_time" nolabel="1" class="oe_inline"/> minutes
                                        </div>
                                    </div>
                                    <!-- Scoring -->
                                    <field name="scoring_type" widget="radio" force_save="1"/>
                                    <field name="scoring_success_min" invisible="scoring_type == 'no_scoring'"/>
                                    <label for="certification" invisible="survey_type == 'live_session' or scoring_type == 'no_scoring'"/>
                                    <div class="o_checkbox_optional_field" name="certification" invisible="survey_type == 'live_session'">
                                        <field name="certification" invisible="scoring_type == 'no_scoring'"/>
                                        <div invisible="not certification" class="w-100">
                                            <field name="certification_report_layout" placeholder="Pick a Style..." class="w-50"/>
                                            <button name="action_survey_preview_certification_template" string="Preview" type="object" icon="fa-external-link" target="_blank" class="btn-link pt-0"/>
                                        </div>
                                    </div>
                                    <field name="certification_mail_template_id" placeholder="Pick a Template..." invisible="not certification"/>
                                    <label for="certification_give_badge" invisible="not certification or not users_login_required"/>
                                    <div class="float-start o_checkbox_optional_field" invisible="not certification or not users_login_required">
                                        <field name="certification_give_badge"/>
                                        <div invisible="not certification_give_badge">
                                            <field name="certification_badge_id" placeholder="Pick a Badge..." invisible="not certification_give_badge or certification_badge_id" required="certification_give_badge" domain="[('survey_id', '=', id), ('survey_id', '!=', False)]" context="{'default_name': title,                                                         'default_description': 'Congratulations, you have succeeded this certification',                                                         'default_rule_auth': 'nobody',                                                         'default_level': None,                                                         'form_view_ref': 'survey.gamification_badge_form_view_simplified'}"/>
                                            <field name="certification_badge_id_dummy" invisible="not certification_give_badge or not certification_badge_id" placeholder="Pick a Badge..." options="{'no_create': True}" context="{'form_view_ref': 'survey.gamification_badge_form_view_simplified'}"/>
                                        </div>
                                    </div>
                                </group>
                                <group string="Live Session" name="live_session" invisible="not session_available">
                                    <field name="access_token" invisible="1"/>  <!-- dependency of 'session_code' -->
                                    <field name="session_code"/>
                                    <field name="session_link" widget="CopyClipboardChar"/>
                                    <field name="session_speed_rating" invisible="scoring_type == 'no_scoring'"/>
                                    <field name="session_speed_rating_time_limit" invisible="scoring_type == 'no_scoring' or not session_speed_rating"/>
                                </group>
                            </group>
                        </page>
                        <page string="Description" name="description">
                            <field name="description" placeholder="e.g. &quot;The following Survey will help us...&quot;" options="{'embedded_components': false}" nolabel="1"/>
                        </page>
                        <page string="End Message" name="description_done">
                            <field name="description_done" placeholder="e.g. &quot;Thank you very much for your feedback!&quot;" options="{'embedded_components': false}" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>