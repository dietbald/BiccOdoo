<xpath expr="//t[@name='action_content']" position="inside">
                <group invisible="state != 'whatsapp'">
                    <group>
                        <field name="wa_template_id" placeholder="Choose a template..." required="state == 'whatsapp'" options="{'no_create': True}"/>
                    </group>
                </group>
            </xpath>