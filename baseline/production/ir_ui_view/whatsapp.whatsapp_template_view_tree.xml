<list string="WhatsApp Template" default_order="sequence asc, write_date desc, id asc">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="model_id"/>
                <field name="wa_account_id" groups="whatsapp.group_whatsapp_admin" optional="hidden"/>
                <field name="template_name" optional="hidden"/>
                <field name="header_type" optional="hidden"/>
                <field name="create_uid" widget="many2one_avatar"/>
                <field name="create_date" optional="hidden"/>
                <field name="quality" widget="priority" optional="hidden" readonly="1"/>
                <field name="status" decoration-success="status == 'approved'" decoration-danger="status == 'rejected'" widget="badge"/>
            </list>