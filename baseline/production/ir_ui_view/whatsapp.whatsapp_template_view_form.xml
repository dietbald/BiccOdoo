<form string="WhatsApp Template">
                <header>
                    <field name="has_action" invisible="1"/>
                    <button name="button_reset_to_draft" string="Reset to draft" type="object" invisible="status not in ['rejected', 'approved']" groups="whatsapp.group_whatsapp_admin"/>
                    <button name="button_submit_template" string="Submit for Approval" class="btn btn-primary" type="object" invisible="not wa_account_id or status != 'draft'" groups="whatsapp.group_whatsapp_admin"/>
                    <button name="button_create_action" string="Allow Multi" class="btn btn-primary" type="object" invisible="has_action or status != 'approved'" groups="whatsapp.group_whatsapp_admin"/>
                    <button name="button_delete_action" string="Disallow Multi" type="object" groups="whatsapp.group_whatsapp_admin" invisible="not has_action or status != 'approved'" confirm="There might be other templates that still need the Multi"/>
                    <button name="button_sync_template" string="Sync Template" type="object" invisible="not wa_template_uid" groups="whatsapp.group_whatsapp_admin"/>
                    <button name="1192" string="Preview" type="action" target="new" context="{'active_id': id, 'dialog_size': 'medium'}"/>
                    <field name="status" widget="statusbar" statusbar_visible="draft,pending,approved"/>
                </header>
                <div class="alert alert-warning pb8" role="alert" invisible="not warning_message">
                    <field name="warning_message"/>
                </div>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button type="object" name="action_open_messages" class="oe_stat_button" icon="fa-whatsapp">
                            <field name="messages_count" string="Messages" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" required="1" placeholder="e.g. &quot;Send Order Document&quot;"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="active" invisible="1"/>
                            <field name="wa_account_id" groups="whatsapp.group_whatsapp_admin" readonly="status != 'draft'" placeholder="Pick an Account..."/>
                            <field name="model" invisible="1"/>
                            <field name="model_id"/>
                            <field name="phone_field" widget="field_selector" options="{'model': 'model'}"/>
                            <field name="template_type" readonly="status != 'draft'"/>
                            <field name="allowed_user_ids" widget="many2many_tags_avatar" placeholder="Accessible to all Users"/>
                        </group>
                        <group>
                            <field name="template_name" readonly="not wa_template_uid or status != 'draft'"/>
                            <field name="lang_code" readonly="status != 'draft'"/>
                            <field name="header_type" readonly="status != 'draft'" required="1"/>
                            <field name="header_text" invisible="header_type != 'text'" required="header_type == 'text'" readonly="status != 'draft'" placeholder="e.g. Invitation for {{1}}"/>
                            <field name="header_attachment_ids" widget="many2many_binary" options="{'accepted_file_extensions': 'image/jpeg, image/png', 'number_of_files': 1}" invisible="header_type != 'image'"/>
                            <field name="header_attachment_ids" widget="many2many_binary" options="{                                     'accepted_file_extensions': 'text/plain, application/pdf, application/vnd.ms-powerpoint, application/msword, application/vnd.ms-excel,                                     application/vnd.openxmlformats-officedocument.wordprocessingml.document, application/vnd.openxmlformats-officedocument.presentationml.presentation,                                     application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',                                     'number_of_files': 1                                 }" invisible="header_type != 'document' or report_id != False"/>
                            <field name="header_attachment_ids" widget="many2many_binary" options="{'accepted_file_extensions': 'video/mp4', 'number_of_files': 1}" invisible="header_type != 'video'"/>
                            <field name="report_id" invisible="header_type != 'document' or header_attachment_ids"/>
                            <field name="footer_text" readonly="status != 'draft'"/>
                            <field name="quality" widget="priority" invisible="quality == 'none'" readonly="1"/>
                            <field name="wa_template_uid" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page id="body" name="Body">
                            <field name="body" widget="whatsapp_text_variables" placeholder="Hello {{1}}, here is your order with the reference {{2}} ..." readonly="status != 'draft'" required="1"/>
                        </page>
                        <page id="buttons" name="Buttons">
                            <field name="button_ids" readonly="status != 'draft'">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="button_type"/>
                                    <field name="name" required="1"/>
                                    <field name="call_number" readonly="button_type != 'phone_number'" required="button_type == 'phone_number'" invisible="button_type != 'phone_number'"/>
                                    <field name="website_url" readonly="button_type != 'url'" required="button_type == 'url'" invisible="button_type != 'url'" help="If the URL has no domain, your domain will be added." placeholder="e.g. https://www.example.com"/>
                                    <field name="url_type" required="button_type == 'url'" invisible="button_type != 'url'"/>
                                </list>
                            </field>
                        </page>
                        <page id="variables" name="Variables">
                            <field name="variable_ids">
                                <list editable="bottom" create="false" delete="false">
                                    <field name="display_name" string="Name"/>
                                    <field name="demo_value" required="1"/>
                                    <field name="field_type"/>
                                    <field name="field_name" widget="field_selector" required="field_type == 'field'" readonly="field_type != 'field'" invisible="field_type != 'field'" options="{'model': 'model'}"/>
                                    <field name="model" column_invisible="True"/>
                                    <field name="line_type" column_invisible="True" force_save="1"/>
                                    <field name="name" column_invisible="True" force_save="1"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>