<search string="Rental Analysis">
                <field name="start_date" string="Date" filter_domain="[                         ('start_date', '&lt;=', raw_value),                         ('return_date', '&gt;=', raw_value),                     ]"/>
                <filter name="my_sale_orders" string="My Orders" domain="[('order_id.user_id', '=', uid)]"/>
                <separator/>
                <filter name="quotation" string="Quotations" domain="[('order_id.rental_status', '=', 'draft')]"/>
                <filter name="confirmed" string="Confirmed Orders" domain="[('order_id.rental_status', 'not in', ('draft', 'cancel', 'sent'))]"/>
                <filter name="booked" string="Booked" domain="[('rental_status', '=', 'pickup')]"/>
                <filter name="pickedup" string="Picked-up" domain="[('rental_status', '=', 'return')]"/>
                <separator/>
                <!-- Note: the use of 'any' is useful to display "Next Action is in Today" when
                     inspecting the filter. -->
                <filter name="today" string="To Do Today" domain="[('order_id', 'any', [                         '&amp;', ('next_action_date', '&gt;=', 'today'),                                  ('next_action_date', '&lt;', 'today +1d'),                     ])]"/>
                <filter name="late" string="Late" domain="[('is_late', '=', True)]"/>
                <separator/>
                <field name="product_id"/>
                <field name="salesman_id"/>
                <field name="order_partner_id"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <group name="report_group_by">
                    <filter name="groupby_user" string="Salesperson" context="{'group_by': 'salesman_id'}"/>
                    <filter name="groupby_sales_channel" string="Sales Team" context="{'group_by': 'team_id'}"/>
                    <filter name="groupby_customer" string="Customer" context="{'group_by': 'order_partner_id'}"/>
                    <filter name="groupby_country" string="Customer Country" context="{'group_by': 'country_id'}"/>
                    <filter name="groupby_product" string="Product" context="{'group_by': 'product_id'}"/>
                    <filter name="groupby_category" string="Product Category" context="{'group_by': 'categ_id'}"/>
                    <filter name="groupby_company" string="Company" context="{'group_by': 'company_id'}" groups="base.group_multi_company"/>
                    <separator/>
                    <filter name="groupby_pickup_date" string="Pickup Date" context="{'group_by': 'start_date'}"/>
                    <filter name="groupby_return_date" string="Return Date" context="{'group_by': 'return_date'}"/>
                </group>
            </search>