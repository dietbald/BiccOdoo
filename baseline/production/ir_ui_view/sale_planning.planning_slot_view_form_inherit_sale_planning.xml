<data><field name="start_datetime" position="attributes">
                <attribute name="readonly">sale_line_plannable and not start_datetime</attribute>
                <attribute name="required">not sale_line_plannable or end_datetime</attribute>
            </field>
            <field name="end_datetime" position="attributes">
                <attribute name="readonly">sale_line_plannable and not end_datetime</attribute>
                <attribute name="required">not sale_line_plannable or start_datetime</attribute>
            </field>
            <field name="role_id" position="after">
                <field name="sale_order_state" invisible="1"/>
                <label for="sale_line_id"/>
                <div class="o_row">
                    <field name="sale_line_id" groups="!sales_team.group_sale_salesman" options="{'no_create': True, 'no_open': True}" context="{'with_planning_remaining_hours': True, 'with_price_unit': True}"/>
                    <field name="sale_line_id" groups="sales_team.group_sale_salesman" options="{'no_create': True}" context="{'with_planning_remaining_hours': True, 'with_price_unit': True}"/>
                    <span class="fa fa-exclamation-triangle text-warning" title="The sales order associated with this shift has been cancelled. We recommend either updating the sales order item or unscheduling this shift in line with the cancellation of the sales order." invisible="sale_order_state != 'cancel'"/>
                </div>
                <field name="sale_line_plannable" invisible="1"/>
            </field>
            <xpath expr="//div[@name='button_box']" position="inside">
                <field name="sale_order_id" invisible="1"/>
                <button class="oe_stat_button" type="object" name="action_view_sale_order" icon="fa-dollar" invisible="not sale_order_id" groups="sales_team.group_sale_salesman_all_leads">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Sales Order</span>
                    </div>
                </button>
            </xpath>
        </data>