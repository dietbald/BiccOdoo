<form string="Salary Package Offer">
                <header>
                    <button name="action_send_by_email" type="object" data-hotkey="shift+e" string="Send By Email" class="oe_highlight" invisible="not id or state in ['refused', 'expired', 'cancelled'] or not sign_template_id"/>
                    <button data-hotkey="shift+e" string="Send By Email" class="oe_highlight disabled" invisible="not id or state in ['refused', 'expired', 'cancelled'] or sign_template_id"/>
                    <button name="action_jump_to_offer" type="object" data-hotkey="shift+g" string="Salary Configurator" class="btn btn-secondary" invisible="not id or state in ['refused', 'expired', 'cancelled'] or not sign_template_id"/>
                    <button data-hotkey="shift+g" string="Salary Configurator" class="btn btn-secondary disabled" invisible="not id or state in ['refused', 'expired', 'cancelled'] or sign_template_id"/>
                    <field name="url" class="btn btn-addr ps-0" string="Copy link" widget="CopyClipboardButton" options="{'btn_class': 'btn btn-secondary'}" invisible="not id or not sign_template_id"/>
                    <field name="url" class="btn btn-addr ps-0 disabled" string="Copy link" widget="CopyClipboardButton" options="{'btn_class': 'btn btn-secondary'}" invisible="not id or sign_template_id"/>
                    <field name="state" widget="statusbar" invisible="not is_half_sign_state_required" statusbar_visible="open,half_signed,full_signed" readonly="1"/>
                    <field name="state" widget="statusbar" invisible="is_half_sign_state_required" statusbar_visible="open,full_signed" readonly="1"/>
                </header>
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button name="action_view_version" type="object" class="oe_stat_button" icon="fa-book" invisible="state != 'half_signed'">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Contract Details</span>
                            </div>
                        </button>
                        <button name="action_view_signature_request" type="object" data-hotkey="shift+g" class="oe_stat_button" icon="fa-pencil" invisible="state != 'half_signed' or not sign_request_ids">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Requested Signature</span>
                            </div>
                        </button>

                        <button name="action_view_version" type="object" data-hotkey="shift+g" class="oe_stat_button" icon="fa-book" invisible="state != 'full_signed'">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_text">Signed Contract</span>
                                </div>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Refused" bg_color="text-bg-danger" invisible="state != 'refused'"/>
                    <widget name="web_ribbon" title="Cancelled" bg_color="text-bg-danger" invisible="state != 'cancelled'"/>
                    <div class="alert alert-info text-center" role="status" invisible="state != 'full_signed'">
                        This offer has been fully signed and can no longer be edited. However, you can still make changes to the Signed Contract if needed.
                    </div>
                    <div role="alert" class="alert alert-danger" invisible="sign_template_id">
                        Please define a signature template on the contract before generating the offer.
                    </div>
                    <div class="oe_title pe-0">
                        <label for="display_name"/>
                        <h1>
                            <field name="display_name" options="{'line_breaks': False}" readonly="state == 'cancelled'" placeholder="e.g. Offer 70k Employer Cost"/>
                        </h1>
                    </div>
                    <group>
                        <group name="group_offer">
                            <field name="contract_template_id" readonly="state in ['cancelled', 'full_signed']" placeholder="Select a contract template to load its predefined values"/>
                            <label for="sign_template_id" string="PDF Template"/>
                            <div class="o_row">
                                <field name="sign_template_id" options="{'no_create': True}" readonly="contract_template_id" placeholder="Select the PDF that the employee will sign"/>
                                <button name="action_edit_offer_signatories" type="object" class="btn btn-secondary" string="Edit Signatories" invisible="sign_template_id" disabled="1"/>
                                <button name="action_edit_offer_signatories" type="object" class="btn btn-secondary" string="Edit Signatories" invisible="not sign_template_id"/>
                            </div>
                            <field name="final_yearly_costs" readonly="state in ['cancelled', 'full_signed']"/>
                            <field name="job_title" readonly="state == 'cancelled'" placeholder="e.g. Lead Dev, Junior Consultant"/>
                            <field name="employee_job_id" readonly="state in ['cancelled', 'full_signed']" placeholder="e.g. Developer, Sales, Consultant"/>
                            <field name="department_id" readonly="state == 'cancelled'" placeholder="e.g. Human Resources, Research &amp; Development, Marketing"/>
                            <label for="contract_start_date" string="Contract Date"/>
                            <div class="o_row">
                                <field name="contract_start_date" readonly="state in ['cancelled', 'full_signed']"/>
                                <i class="fa fa-long-arrow-right mx-2" aria-label="Arrow icon" title="Arrow"/>
                                <field name="contract_end_date" readonly="state in ['cancelled', 'full_signed']" placeholder="No limit"/>
                            </div>
                            <field name="offer_create_date" readonly="1"/>
                            <field name="offer_end_date" readonly="state in ['cancelled', 'full_signed']" placeholder="Always valid"/>
                        </group>
                        <group name="group_offer_to">
                            <field name="employee_id" invisible="applicant_id or not context.get('is_hr_payroll', True)" required="context.get('is_hr_payroll')" domain="[('company_id', 'in', allowed_company_ids)]" readonly="not context.get('is_hr_payroll', False) or state == 'full_signed'" placeholder="Select an employee..."/>
                            <field name="applicant_id" invisible="employee_version_id or context.get('is_hr_payroll', False)" required="not context.get('is_hr_payroll')" domain="[('company_id', 'in', allowed_company_ids)]" readonly="context.get('is_hr_payroll', True) or state == 'full_signed'" context="{'show_partner_name': True}" placeholder="Select an applicant..."/>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="refusal_reason" readonly="1" invisible="not refusal_reason"/>
                            <field name="refusal_date" readonly="1" invisible="not refusal_date"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                        <group string="Token" name="group_token" groups="base.group_no_one">
                            <field name="access_token"/>
                            <field name="create_date"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>