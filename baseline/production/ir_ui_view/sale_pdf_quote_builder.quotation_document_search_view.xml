<search string="Quotation Document">
                <field name="name"/>
                <separator/>
                <group>
                    <filter string="Document type" name="doc_type" context="{'group_by': 'document_type'}"/>
                    <filter string="Quotation Template" name="quotation_template" context="{'group_by': 'quotation_template_ids'}"/>
                    <filter name="all" string="All"/>
                    <filter name="archived" string="Archived" domain="[('active', '=', False)]"/>
                    <filter name="add_by_default" string="Add By Default" domain="[('add_by_default', '=', True)]"/>
                </group>
            </search>