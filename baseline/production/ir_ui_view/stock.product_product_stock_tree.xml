<list sample="1" js_class="stock_report_list_view" duplicate="0" default_order="is_favorite desc, default_code, name, id">
                    <header>
                        <button name="1280" invisible="((context.get('inventory_mode') and not context.get('inventory_report_mode')) or context.get('no_at_date'))" string="Inventory at Date" type="action" class="btn-primary ms-1" display="always"/>
                    </header>
                    <field name="id" column_invisible="True"/>
                    <field name="display_name" string="Product"/>
                    <field name="categ_id" optional="hide"/>
                    <!-- <button name="705" type="action" class="btn-link"
                            context="{'search_default_product_id': id, 'default_product_id': id}"> -->
                    <field name="qty_available" string="On Hand" sum="Total On Hand" widget="stock_action_field" options="{ 'action_name': 'stock.action_view_inventory_tree' }" context="{ 'search_default_product_id': id, 'default_product_id': id }"/>
                    <field name="free_qty" string="Free to Use" sum="Total Free to Use"/>
                    <field name="incoming_qty" optional="show" sum="Total Incoming"/>
                    <field name="outgoing_qty" optional="show" sum="Total Outgoing"/>
                    <field name="virtual_available" string="Forecasted" optional="hide" sum="Total Forecasted"/>
                    <field name="uom_id" widget="many2one_uom" groups="uom.group_uom" options="{'no_create': True}"/>
                    <button name="714" string="History" type="action" class="btn-link" icon="fa-history" context="{'search_default_product_id': id, 'default_product_id': id}"/>
                    <button name="action_view_orderpoints" string="Replenishment" type="object" class="btn-link" icon="fa-refresh" context="{'search_default_product_id': id, 'is_stock_report': True}"/>
                    <button name="706" string="Locations" type="action" class="btn-link" groups="stock.group_stock_multi_locations" icon="fa-cubes" context="{'search_default_product_id': id, 'default_product_id': id}" invisible="qty_available == 0"/>
                    <button name="action_product_forecast_report" string="Forecast" type="object" class="btn-link" icon="fa-area-chart" context="{'default_product_id': id}" invisible="incoming_qty == 0 and outgoing_qty == 0"/>
                </list>