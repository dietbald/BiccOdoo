<form string="New Transaction">
                    <header>
                        <button name="action_button_draft" type="object" class="btn btn-primary" string="Reset to Draft" invisible="state == 'draft'"/>
                    </header>
                    <field name="state" invisible="1"/>
                    <field name="statement_complete" invisible="1"/>
                    <field name="statement_valid" invisible="1"/>
                    <field name="is_reconciled" invisible="1"/>
                    <field name="suitable_journal_ids" invisible="1"/>
                    <field name="currency_id" invisible="1"/>
                    <field name="company_id" invisible="1"/>
                    <sheet>
                        <group>
                            <group>
                                <field name="date" readonly="is_reconciled and amount != 0"/>
                                <field name="payment_ref" required="1" readonly="is_reconciled and amount != 0"/>
                                <field name="partner_id" readonly="is_reconciled and amount != 0"/>
                            </group>
                            <group>
                                <field name="amount" readonly="is_reconciled and amount != 0"/>
                                <field name="foreign_currency_id" groups="base.group_multi_currency" domain="[('id', '!=', currency_id)]" options="{'no_open': True, 'no_create': True}" readonly="is_reconciled and amount != 0"/>
                                <field name="amount_currency" groups="base.group_multi_currency" invisible="not foreign_currency_id" readonly="is_reconciled and amount != 0"/>
                                <field name="journal_id" domain="[('type', 'in', ['bank', 'cash'])]" readonly="context.get('default_journal_id') or (is_reconciled and amount != 0)"/>
                            </group>
                        </group>
                        <footer>
                            <button string="Save &amp; Close" type="object" name="action_save_close" class="btn btn-primary" data-hotkey="q"/>
                            <button string="Save &amp; New" type="object" name="action_save_new" class="btn btn-primary" data-hotkey="n"/>
                            <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                        </footer>
                    </sheet>
                </form>