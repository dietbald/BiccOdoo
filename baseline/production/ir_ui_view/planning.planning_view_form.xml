<form js_class="planning_form">
                <header>
                    <button string="Publish &amp; Send" type="object" name="action_send" class="btn btn-primary" close="1" groups="planning.group_planning_manager" data-hotkey="q" invisible="context.get('my_planning_action') or (state == 'published' or resource_type == 'material')"/>
                    <button name="action_self_assign" class="btn btn-primary" type="object" string="I Take It" groups="!planning.group_planning_manager" close="1" invisible="not context.get('my_planning_action') or not is_users_role or is_past or not start_datetime or not end_datetime or resource_id and not request_to_switch or user_id == uid and (allow_self_unassign or request_to_switch)"/>
                    <button name="action_self_assign" class="btn btn-secondary" type="object" string="I Take It" groups="!planning.group_planning_manager" close="1" invisible="not context.get('my_planning_action') or is_users_role or is_past or not start_datetime or not end_datetime or resource_id and not request_to_switch or user_id == uid and (allow_self_unassign or request_to_switch)"/>
                    <button name="action_switch_shift" class="btn btn-secondary" type="object" string="Ask To Switch" groups="!planning.group_planning_manager" invisible="user_id != uid or is_past or request_to_switch or allow_self_unassign"/>
                    <button name="action_cancel_switch" class="btn btn-secondary" type="object" string="Cancel Switch" groups="!planning.group_planning_manager" invisible="not start_datetime or not end_datetime or not request_to_switch or allow_self_unassign or user_id != uid and request_to_switch" close="1"/>

                    <button name="action_self_unassign" class="btn btn-secondary" type="object" string="I Am Unavailable" invisible="not context.get('my_planning_action') or (user_id != uid or not allow_self_unassign or is_past or is_unassign_deadline_passed or resource_type == 'material')"/>
                    <button string="Send" type="object" name="action_send" class="btn btn-secondary" close="1" groups="planning.group_planning_manager" data-hotkey="v" invisible="context.get('my_planning_action') or (state == 'draft' or resource_type == 'material')"/>
                    <button string="Reset to Draft" type="object" name="action_unpublish" class="btn btn-secondary" close="1" groups="planning.group_planning_manager" invisible="state == 'draft' or resource_type == 'material'" data-hotkey="q"/>
                    <button string="Auto Plan" type="object" name="auto_plan_id" class="btn btn-secondary" groups="planning.group_planning_manager" data-hotkey="p" invisible="context.get('my_planning_action') or resource_id or not start_datetime or not end_datetime"/>
                    <field name="state" widget="statusbar" groups="planning.group_planning_manager" invisible="context.get('my_planning_action')"/>
                </header>
                <field name="is_past" invisible="1"/>
                <field name="is_unassign_deadline_passed" invisible="1"/>
                <field name="work_email" invisible="1"/>
                <field name="previous_template_id" invisible="1"/>
                <field name="template_reset" invisible="1"/>
                <field name="employee_id" invisible="1"/>
                <field name="user_id" invisible="1"/>
                <field name="resource_type" invisible="1"/>
                <field name="is_users_role" invisible="1"/>
                <field name="request_to_switch" invisible="1"/>
                <field name="overlap_slot_count" invisible="1"/>
                <field name="repeat" invisible="1"/>
                <field name="color" invisible="1"/>
                <div class="alert alert-info oe_edit_only d-lg-flex align-items-center" role="status" invisible="not recurrency_id or not repeat">
                    <div class="my-2">
                        <field name="recurrence_update" widget="radio" class="mb-0 d-sm-block d-lg-none" options="{'horizontal': false}"/>
                        <field name="recurrence_update" widget="radio" class="mb-0 d-lg-block d-none" options="{'horizontal': true}"/>
                    </div>
                </div>
                <div role="alert" class="alert alert-warning text-center py-2 mb-2" invisible="overlap_slot_count == 0 and (not request_to_switch or allow_self_unassign)">
                    <field name="conflicting_slot_ids" widget="conflicting_slot_ids" invisible="not conflicting_slot_ids.length"/>
                    <p class="align-middle mb-0" invisible="not request_to_switch or allow_self_unassign">
                        <span>The employee assigned would like to switch shifts with someone else.</span>
                    </p>
                </div>

                <sheet string="Shift">
                    <div class="oe_button_box" name="button_box">
                        <!-- Used by other modules-->
                    </div>
                    <field name="allow_self_unassign" invisible="1"/>
                    <field name="template_id" domain="[['id', 'in', template_autocomplete_ids]]" widget="selection_badge" options="{'horizontal': true}" groups="planning.group_planning_manager" invisible="not template_autocomplete_ids and not template_id"/>
                    <field name="template_autocomplete_ids" invisible="1"/>
                    <field name="recurrency_id" invisible="1"/>
                    <group>
                        <group>
                            <field name="resource_id" context="{'search_view_ref': 'planning.resource_resource_search_view_roles', 'search_default_role_ids': role_id, 'from_planning': True, 'show_job_title': True, 'list_view_ref': 'planning.resource_resource_tree_view_inherit', 'form_view_ref': 'planning.resource_resource_with_employee_form_view_inherit'}" readonly="not start_datetime or not end_datetime" widget="many2one_avatar_resource" placeholder="Open Shift"/>
                            <field name="role_id" context="{'search_default_resource_ids': resource_id}" placeholder="e.g. Bartender"/>
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                            <field name="company_id" invisible="1"/>
                        </group>
                        <!-- slot_info_right group is used in another module -->
                        <group name="slot_info_right">
                            <label for="start_datetime"/>
                            <div class="d-flex">
                                <field name="start_datetime" string="Date" widget="daterange" options="{'end_date_field': 'end_datetime'}"/>
                                <field name="end_datetime" invisible="1"/>
                                <field name="repeat" nolabel="1" style="width: fit-content;font-size:10px;" widget="boolean_icon" options="{'icon': 'fa-repeat'}" groups="planning.group_planning_manager"/>
                            </div>
                            <label for="repeat_interval" invisible="not repeat"/>
                            <div invisible="not repeat" class="d-flex">
                                <field name="repeat_interval" class="me-2" style="max-width: 2rem !important;"/>
                                <field name="repeat_unit" class="me-2" style="max-width: 4rem !important;"/>
                                <field name="repeat_type" required="repeat" nolabel="1" class="me-2" style="max-width: 15rem !important;"/>
                                <field name="repeat_until" nolabel="1" invisible="repeat_type != 'until'" required="repeat_type == 'until' and repeat" class="me-2"/>
                                <field name="repeat_number" class="me-2" invisible="repeat_type != 'x_times'" required="repeat_type == 'x_times' and repeat" style="max-width: 2rem !important;"/>
                            </div>
                            <label for="allocated_hours"/>
                            <div class="o_row">
                                <field name="allocated_hours" widget="float_time" class="oe_inline" required="resource_type == 'material'"/>
                                <span invisible="resource_type == 'material'">
                                    (<field name="allocated_percentage" class="o_field_percentage oe_inline" decoration-danger="allocated_percentage &gt; 100"/> %)
                                </span>
                            </div>
                        </group>
                    </group>
                    <field name="slot_properties" columns="2"/>
                    <field name="allow_template_creation" invisible="1"/>
                    <field name="name" nolabel="1" placeholder="Additional note sent to the employee"/>
                </sheet>
            </form>