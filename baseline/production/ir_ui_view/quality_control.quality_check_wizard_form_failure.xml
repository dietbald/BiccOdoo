<form string="Quality Check Failed">
                <field name="test_type" invisible="1"/>
                <field name="measure_on" invisible="1"/>
                <field name="show_lot_text" invisible="1"/>
                <field name="product_tracking" invisible="1"/>
                <field name="potential_failure_location_ids" invisible="1"/>
                <field name="product_id" invisible="1"/>
                <group>
                    <group invisible="measure_on != 'move_line' and not potential_failure_location_ids">
                        <label for="qty_failed" string="Quantity Failed" invisible="measure_on != 'move_line'"/>
                        <div invisible="measure_on != 'move_line'">
                            <field name="qty_failed" required="True" class="oe_inline"/>
                            <field name="uom_id" widget="many2one_uom" class="oe_inline"/>
                        </div>
                        <field name="lot_name" string="Lot/SN" invisible="measure_on != 'move_line' or not show_lot_text or product_tracking == 'none'"/>
                        <field name="lot_line_id" string="Lot/SN" invisible="measure_on != 'move_line' or show_lot_text or product_tracking == 'none'"/>
                        <field name="failure_location_id" invisible="not potential_failure_location_ids" widget="selection_badge" domain="[('id', 'in', potential_failure_location_ids)]"/>
                    </group>
                    <group>
                        <div class="alert" role="alert" colspan="2" invisible="test_type != 'measure'">
                            <field name="warning_message" nolabel="1"/>
                        </div>
                        <div colspan="2">
                            <field name="failure_message" nolabel="1"/>
                        </div>
                    </group>
                </group>
                <footer>

                    <button name="confirm_measure" type="object" class="btn-primary" string="Confirm Measure" invisible="test_type != 'measure'" data-hotkey="w"/>
                    <button name="confirm_fail" type="object" class="btn-primary" string="Confirm" invisible="test_type == 'measure'" data-hotkey="w"/>
                    <button name="correct_measure" type="object" class="btn-secondary" string="Correct Measure" invisible="test_type != 'measure'" data-hotkey="q"/>
                    <button name="action_generate_previous_window" type="object" class="btn-secondary" string="Back" invisible="test_type == 'measure'" data-hotkey="q"/>
                </footer>
            </form>