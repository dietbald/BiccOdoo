<data><xpath expr="//field[@name='unit_amount']" position="after">
                    <field name="validated" invisible="1"/>
                    <field name="user_can_validate" invisible="1"/>
                    <field name="timer_start" invisible="1"/>
                </xpath>
                <xpath expr="//field[@name='unit_amount']" position="attributes">
                    <attribute name="readonly">id and (readonly_timesheet or timer_start)</attribute>
                </xpath>
                <xpath expr="//sheet" position="before">
                    <header>
                        <button string="Validate" name="action_validate_timesheet" type="object" class="btn btn-primary" invisible="validated or not user_can_validate" context="{'use_notification': False}" data-hotkey="q"/>
                        <button string="Reset to draft" name="action_invalidate_timesheet" type="object" class="btn btn-secondary" invisible="not validated or not user_can_validate" context="{'use_notification': False}" data-hotkey="r"/>
                        <field name="validated_status" widget="statusbar" groups="!base.group_portal"/>
                    </header>
                </xpath>
            </data>