<search string="Survey">
                <field string="Survey" name="title" filter_domain="['|', ('title', 'ilike', self), ('session_code', 'ilike', self)]"/>
                <field string="Question &amp; Pages" name="question_and_page_ids"/>
                <field name="user_id"/>
                <field name="restrict_user_ids"/>
                <filter string="Is a Certification" name="certification" domain="[('certification', '=', True)]"/>
                <filter string="Is not a Certification" name="not_certification" domain="[('certification', '=', False)]"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <separator/>
                <filter invisible="1" string="My Activities" name="filter_activities_my" domain="[('activity_user_id', '=', uid)]"/>
                <separator invisible="1"/>
                <filter invisible="1" string="Late Activities" name="activities_overdue" domain="[('my_activity_date_deadline', '&lt;', 'today')]" help="Show all records whose next activity date is past"/>
                <filter invisible="1" string="Today Activities" name="activities_today" domain="[('my_activity_date_deadline', '=', 'today')]"/>
                <filter invisible="1" string="Upcoming Activities" name="activities_upcoming_all" domain="[('my_activity_date_deadline', '&gt;', 'today')]"/>
                <group>
                    <filter string="Responsible" name="group_by_responsible" context="{'group_by': 'user_id'}"/>
                    <filter name="group_by_restrict_user_ids" context="{'group_by': 'restrict_user_ids'}"/>
                </group>
            </search>