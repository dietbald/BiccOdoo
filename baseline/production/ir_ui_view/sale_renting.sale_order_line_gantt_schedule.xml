<gantt string="Rental" js_class="schedule_gantt" date_start="start_date" date_stop="return_date" default_group_by="product_id" groups_limit="20" create="true" edit="true" plan="false" cell_create="true" on_create="sale_renting.action_create_rental_order" decoration-info="state in ['draft', 'sent']" decoration-danger="is_late" color="rental_color" consolidation="product_uom_qty" consolidation_max="{'product_id': 1e9}" thumbnails="{'order_partner_id': 'avatar_128', 'salesman_id': 'avatar_128'}" form_view_id="6335" sample="false">
                <templates>
                    <div t-name="gantt-popover">
                        <div>
                            <strong>Order # — </strong>
                            <t t-out="order_id.display_name"/>
                        </div>
                        <div>
                            <strong>Pickup — </strong>
                            <t t-out="start_date.toFormat('f')"/>
                        </div>
                        <div>
                            <strong>Return — </strong>
                            <t t-out="return_date.toFormat('f')"/>
                        </div>
                        <div>
                            <strong>Status — </strong>
                            <t t-if="state != 'sale'">Quotation</t>
                            <t t-elif="rental_status == 'pickup' and is_late">Late Pickup</t>
                            <t t-elif="rental_status == 'pickup'">Booked</t>
                            <t t-elif="rental_status == 'return' and is_late">Late Return</t>
                            <t t-elif="rental_status == 'return'">Picked-Up</t>
                            <t t-elif="rental_status == 'returned'">Returned</t>
                        </div>
                    </div>
                </templates>
                <field name="order_id"/>
                <field name="state"/>
                <field name="rental_status"/>
                <field name="is_late"/>
            </gantt>