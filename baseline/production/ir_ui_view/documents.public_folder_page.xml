<t name="Public Folder Page" t-name="documents.public_folder_page">
        <t t-call="documents.public_page_layout">
            <t t-set="document" t-value="folder"/>  <!-- for public_page_layout -->
            <t t-set="shareType" t-value="'o_share_folder'"/>

            <t t-set="folder_downloadzip_url" t-valuef="/documents/content/#{quote(folder.access_token)}"/>
            <t t-set="folder_upload_url" t-valuef="/documents/upload/#{quote(folder.access_token)}"/>

            <section class="o_docs_share_multiple_bar d-flex mt-5 mb-4">
                <div class="flex-grow-1 mb16">
                    <h2>
                        <t t-esc="len(documents.filtered(lambda doc: doc.type == 'folder'))"/>
                        folders,
                        <t t-esc="len(documents.filtered(lambda doc: doc.type == 'binary' and (doc.shortcut_document_id or doc).attachment_id))"/>
                        files
                    </h2>
                    <span class="align-middle">
                        shared by
                        <img class="o_author rounded align-middle object-fit-cover" t-attf-src="/documents/avatar/#{quote(folder.access_token)}" t-att-alt="author" t-att-title="author"/>
                        <b t-esc="folder.owner_id.name"/>
                    </span>
                </div>
                <div class="flex-grow-1 text-end">
                    <div class="row g-0 justify-content-end">
                        <div class="col me-3">
                            <form class="o_docs_upload_wrapper position-relative d-inline-block" t-if="folder.access_via_link == 'edit' or folder.user_permission == 'edit'" enctype="multipart/form-data" t-att-action="folder_upload_url" method="POST">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <button title="Upload" class="btn btn-primary"><i class="fa fa-upload"/>  Upload</button>
                                <input name="ufile" multiple="multiple" type="file" onchange="form.submit()"/>
                            </form>
                        </div>
                        <div>
                            <a t-att-href="folder_downloadzip_url" title="Download folder as zip" t-attf-class="btn  #{(folder.access_via_link == 'edit' or folder.user_permission == 'edit') and 'o_docs_btn' or 'btn-primary'}">
                                <i class="fa fa-download fa-fw"/> Download All
                            </a>
                        </div>
                    </div>
                </div>
            </section>

            <div class="o_docs_cards_container row mb32">
                <t t-foreach="documents.filtered(lambda doc: doc.type == 'folder')" t-as="subfolder">
                    <t t-set="subfolder_open_url" t-valuef="/documents/redirect/#{quote(subfolder.access_token)}"/>
                    <t t-set="subfolder_downloadzip_url" t-valuef="/documents/content/#{quote(subfolder.access_token)}"/>
                    <div class="col-sm-6 col-lg-3">
                        <article id="documents-folder" class="o_card d-flex mb16 border">
                            <div class="o_card_content d-flex flex-column flex-nowrap flex-grow-1 flex-shrink-1 justify-content-between">
                                <header>
                                    <h6 class="o_card_title mt0 mb4">
                                        <a t-att-href="subfolder_open_url" t-attf-title="Open folder #{subfolder.name}" t-esc="subfolder.name"/>
                                    </h6>
                                </header>
                                <div class="o_card_footer">
                                    <small class="mb0">
                                        <b><time t-field="subfolder.write_date" t-options="{'format':'MMM d, yyyy'}"/></b>
                                        •
                                        <b>
                                            <t t-esc="len(subfolders[subfolder.id])"/>
                                            documents
                                        </b>
                                    </small>
                                </div>
                            </div>
                            <div class="o_card_right d-flex flex-column flex-nowrap justify-content-between text-end">
                                <img t-attf-title="Owner: #{subfolder.owner_id.name}" t-att-alt="subfolder.owner_id.name" class="object-fit-cover rounded" width="15" height="15" t-attf-src="/documents/avatar/#{quote(subfolder.access_token)}"/>
                                <a t-att-href="subfolder_downloadzip_url" t-attf-title="Download zip #{subfolder.name}">
                                    <i t-attf-class="fa fa-download"/>
                                </a>
                            </div>
                        </article>
                    </div>
                </t>

                <t t-foreach="documents.filtered(lambda doc: doc.type == 'binary')" t-as="document">
                    <t t-set="document_request" t-value="document.type == 'binary' and not document.attachment_id and not document.shortcut_document_id.attachment_id"/>
                    <t t-set="document_content_url" t-valuef="/documents/content/#{quote(document.access_token)}"/>
                    <div class="col-sm-6 col-lg-3">
                        <article id="documents-binary" t-attf-class="o_card d-flex mb16 border                                                #{'o_request_card' if document_request else ''}">
                            <figure class="o_card_left d-flex position-relative flex-column flex-nowrap justify-content-between m-0">
                                <form t-if="(document.access_via_link == 'edit' or document.user_permission == 'edit') and document_request" class="o_hidden" enctype="multipart/form-data" t-attf-action="/documents/upload/#{quote(document.access_token)}" method="POST">
                                    <input name="ufile" t-attf-id="request_{{ document_index }}" type="file" onchange="form.submit()"/>
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                </form>
                                <div t-if="document_request" t-attf-onclick="document.querySelector('#request_{{ document_index }}').click()" t-attf-class="d-flex align-items-center justify-content-center o_request_icon                                                    #{'o_request_allowed' if document.access_via_link == 'edit' or document.user_permission == 'edit' else ''}">
                                    <i class="fa fa-upload fa-2x"/>
                                </div>
                                <div t-else="" class="o_image" t-att-data-mimetype="document.mimetype" t-attf-style="#{document.mimetype and 'image/' in document.mimetype and 'background-image:None;' ''}"/>
                                <div t-if="(document.shortcut_document_id or document).mimetype and 'image/' in (document.shortcut_document_id or document).mimetype" class="o_image_preview" t-attf-style="background-image:url(/documents/thumbnail/#{quote(document.access_token)})"/>
                            </figure>
                            <div class="o_card_content d-flex flex-column flex-nowrap flex-grow-1 flex-shrink-1 justify-content-between">
                                <header>
                                    <h6 class="o_card_title mt0 mb4">
                                        <span t-if="document_request" t-esc="document.name"/>
                                        <a t-else="" t-attf-href="#{document_content_url}?download=0" target="_blank" t-attf-title="Download #{document.name}" t-esc="document.name"/>
                                    </h6>
                                </header>
                                <div class="o_card_footer">
                                    <small class="mb0">
                                        <b><time t-field="document.write_date" t-options="{'format':'MMM d, yyyy'}"/></b>
                                        •
                                        <b>
                                            <t t-if="document_request"><span title="Requested Document">Requested Document</span></t>
                                            <t t-else="" t-call="documents.format_file_size"/>
                                        </b>
                                    </small>
                                </div>
                            </div>
                            <div class="o_card_right d-flex flex-column flex-nowrap justify-content-between text-end">
                                <img t-attf-title="Owner: #{document.owner_id.name}" t-att-alt="document.owner_id.name" class="object-fit-cover rounded" width="15" height="15" t-attf-src="/documents/avatar/#{quote(document.access_token)}"/>
                                <a t-if="not document_request" t-attf-href="#{document_content_url}?download=1" t-attf-title="Download #{document.name}">
                                    <i t-attf-class="fa fa-download"/>
                                </a>
                            </div>
                        </article>
                    </div>
                </t>

                <t t-foreach="documents.filtered(lambda doc: doc.type == 'url')" t-as="document_url">
                    <div class="col-sm-6 col-lg-3">
                        <article id="documents-url" class="o_card o_url_card d-flex mb16 border">
                            <figure class="o_card_left d-flex position-relative flex-column flex-nowrap justify-content-between m-0">
                                <div t-attf-class="d-flex align-items-center justify-content-center o_url_icon">
                                    <i class="fa fa-external-link fa-2x"/>
                                </div>
                            </figure>
                            <div class="o_card_content d-flex flex-column flex-nowrap flex-grow-1 flex-shrink-1 justify-content-between">
                                <header>
                                    <h6 class="o_card_title mt0 mb4">
                                        <a t-att-href="document_url.url" t-attf-title="Open #{document_url.name}" target="blank" t-esc="document_url.name"/>
                                    </h6>
                                </header>
                                <div class="o_card_footer">
                                    <small class="mb0">
                                        <b><time t-field="document_url.write_date" t-options="{'format':'MMM d, yyyy'}"/></b>
                                        •
                                        <b>URL</b>
                                    </small>
                                </div>
                            </div>
                            <div class="o_card_right d-flex flex-column flex-nowrap justify-content-between text-end">
                                <img t-attf-title="Owner: #{document_url.owner_id.name}" t-att-alt="document_url.owner_id.name" class="object-fit-cover rounded" width="15" height="15" t-attf-src="/documents/avatar/#{quote(document_url.access_token)}"/>
                            </div>
                        </article>
                    </div>
                </t>
            </div>
            <div class="text-center col-sm-12 d-sm-none">
                <div class="row g-0 justify-content-center">
                    <div class="me-3">
                        <form class="o_docs_upload_wrapper position-relative d-inline-block" t-if="folder.access_via_link == 'edit' or folder.user_permission == 'edit'" enctype="multipart/form-data" t-att-action="folder_upload_url" method="POST">
                            <button title="Upload" class="btn btn-primary"><i class="fa fa-upload"/>  Upload</button>
                            <input name="ufile" multiple="multiple" type="file" onchange="form.submit()"/>
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        </form>
                    </div>
                    <div>
                        <a t-att-href="folder_downloadzip_url" title="Download all files" t-attf-class="btn #{(folder.access_via_link == 'edit' or folder.user_permission == 'edit') and 'o_docs_btn' or 'btn-primary'}">
                            <i class="fa fa-download fa-fw"/> Download All
                        </a>
                    </div>
                </div>
            </div>
        </t>
    </t>