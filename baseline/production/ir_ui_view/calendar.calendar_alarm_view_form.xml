<form string="Calendar Alarm">
                <sheet>
                    <group>
                        <group name="left_details">
                            <field name="name" invisible="1"/>
                            <field name="alarm_type"/>
                            <field name="mail_template_id" context="{'default_model': 'calendar.event'}" invisible="alarm_type != 'email'" required="alarm_type == 'email'"/>
                            <field name="body" invisible="alarm_type != 'notification'"/>
                        </group>
                        <group name="right_details">
                            <label for="duration"/>
                            <div class="o_row">
                                <field name="duration"/>
                                <field name="interval"/>
                            </div>
                            <field name="notify_responsible" invisible="alarm_type in ('email', 'notification')"/>
                        </group>
                    </group>
                </sheet>
            </form>