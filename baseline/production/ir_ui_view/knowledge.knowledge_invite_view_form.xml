<form class="py-0">
                <field name="article_id" invisible="1"/>
                <field name="have_share_partners" invisible="1"/>
                <div colspan="2" class="alert alert-info text-center" role="alert" invisible="not have_share_partners or permission != 'none'">
                    <i class="fa fa-w fa-info-circle"/> All external users you selected won't be added to the members.
                </div>
                <sheet>
                    <group>
                        <field name="permission" class="w-100"/>
                        <field name="partner_ids" widget="many2many_tags_email" class="o_knowledge_add_people_input w-100" placeholder="Add people or email addresses" options="{'no_quick_create': True}" context="{'show_email': True, 'form_view_ref': 'base.view_partner_simple_form'}"/>
                    </group>
                    <field name="message" placeholder="Extra Comment" invisible="permission == 'none'"/>
                </sheet>
                <footer>
                    <button string="Invite" type="object" name="action_invite_members" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>