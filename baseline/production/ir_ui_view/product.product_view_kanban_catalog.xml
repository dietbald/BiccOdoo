<kanban records_draggable="0" js_class="product_kanban_catalog" default_order="is_favorite desc, default_code, name, id">
                <field name="id"/>
                <templates>
                    <t t-name="menu">
                        <div class="o_product_catalog_cancel_global_click">
                            <a role="menuitem" type="open" class="dropdown-item border-top-0">Edit</a>
                        </div>
                        <div class="container p-0">
                            <field name="is_favorite" widget="boolean_favorite" class="col-12 pe-0"/>
                        </div>
                    </t>
                    <t t-name="card" class="p-0">
                    <!-- TODO : not getting border when select record -->
                        <div class="d-flex flex-grow-1 m-2">
                            <div class="ps-2 d-flex flex-column m-1 overflow-hidden" t-attf-id="product-{{record.id.raw_value}}">
                                <div class="d-flex">
                                    <field name="name" class="fw-bold fs-4 text-reset mb-1 me-2"/>
                                    <field class="mt-1 me-1" name="is_favorite" widget="boolean_favorite" invisible="not is_favorite" nolabel="1" readonly="1"/>
                                </div>
                                <!-- Used by @web/product/js/product_catalog/order_line to
                                        show the price using a t-portal. -->
                                <div name="o_kanban_price" t-attf-id="product-{{record.id.raw_value}}-price"/>
                                <field name="product_template_attribute_value_ids" widget="many2many_tags" domain="[('id', 'in', parent.ids)]" groups="product.group_product_variant" options="{'color_field': 'color'}" invisible="not product_template_attribute_value_ids"/>
                            </div>
                            <field name="image_128" class="ms-auto" invisible="not image_128" widget="image" options="{'size': [55, 55], 'img_class': 'object-fit-contain'}" alt="Product"/>
                        </div>
                    </t>
                </templates>
            </kanban>