<list editable="bottom" js_class="employee_declaration_list">
                <field name="employee_id" options="{'no_create': True, 'no_create_edit': True}" readonly="pdf_file" widget="many2one_avatar_employee"/>
                <field name="res_model" column_invisible="True"/>
                <field name="res_id" column_invisible="True"/>
                <field name="pdf_filename" column_invisible="True"/>
                <field name="pdf_file" readonly="1" filename="pdf_filename" widget="binary"/>
                <field name="pdf_to_generate" optional="hide"/>
                <field name="state" decoration-info="state == 'draft'" decoration-warning="state in ['pdf_to_generate', 'pdf_to_post']" decoration-success="state == 'pdf_generated'" decoration-primary="state == 'pdf_posted'" widget="badge"/>
            </list>