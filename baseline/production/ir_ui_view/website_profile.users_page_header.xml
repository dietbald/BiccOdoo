<t name="Users Page Header" t-name="website_profile.users_page_header">
        <div class="o_wprofile_all_users_header o_wprofile_gradient mb-n5 pb-5">
            <t t-call="website_profile.user_profile_sub_nav"/>
            <div class="container">
                <h1 class="py-4 text-white d-inline-block">All Users</h1>
                <div class="py-4 float-end">
                    <strong class="mb-3 text-white me-2">Rank by:</strong>
                    <div class="mb-3 btn-group">
                        <a t-attf-class="btn btn-light #{ 'active' if group_by == 'week' else ''}" t-att-href="'/profile/users?' + keep_query('search', group_by='week')">This week</a>
                        <a t-attf-class="btn btn-light #{ 'active' if group_by == 'month' else ''}" t-att-href="'/profile/users?' + keep_query('search', group_by='month')">This month</a>
                        <a t-attf-class="btn btn-light #{ 'active' if group_by == 'all' else ''}" t-att-href="'/profile/users?' + keep_query('search')">All time</a>
                    </div>
                </div>
            </div>
        </div>
    </t>