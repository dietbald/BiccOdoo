<data><filter name="date_deadline" position="attributes">
                <attribute name="invisible">context.get('fsm_mode', False)</attribute>
            </filter>
            <filter name="deadline" position="attributes">
                <attribute name="invisible">context.get('fsm_mode', False)</attribute>
            </filter>
        </data>