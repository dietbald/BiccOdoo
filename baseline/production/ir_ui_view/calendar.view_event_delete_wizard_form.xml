<form string="Delete Event">
                <field name="calendar_event_id" invisible="1"/>
                <div col="2" class="alert alert-warning" role="alert">
                    <span>Are you sure you want to delete this event? <br/></span>
                </div>
                <group col="2">
                    <field name="recipient_ids" widget="many2many_tags_email" context="{'force_email': True,' show_email': True, 'no_create_edit': True}"/>
                </group>
                <group col="2">
                    <field name="subject" placeholder="Subject"/>
                </group>
                <field name="body" class="oe-bordered-editor" options="{'style-inline': true}"/>
                <footer>
                    <button string="Send and delete" name="action_send_mail_and_delete" type="object" class="btn-primary o_cw_send_notify_delete"/>
                    <button string="Delete" name="action_delete" type="object" class="btn-primary mx-1"/>
                    <button string="Discard" class="btn-secondary" special="cancel"/>
                </footer>
            </form>