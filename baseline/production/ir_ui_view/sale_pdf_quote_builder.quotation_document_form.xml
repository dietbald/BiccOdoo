<form>
                <sheet>
                    <h1>
                        <field name="name" readonly="not datas"/>
                    </h1>
                    <group>
                        <group>
                            <field name="document_type"/>
                            <label for="datas"/>
                            <div class="o_row">
                                <field name="datas" filename="name" options="{'accepted_file_extensions': '.pdf', 'allowed_mime_type' : 'application/pdf'}" class="oe_inline"/>
                                <button name="action_open_pdf_form_fields" type="object" string="Configure dynamic fields" help="Mark fields as safe to fill in the quote." class="btn btn-link" icon="fa-pencil" colspan="2" groups="base.group_system,base.group_no_one" invisible="not (datas and form_field_ids)"/>
                            </div>
                            <field name="quotation_template_ids" options="{'no_create': True}" widget="many2many_tags"/>
                            <field name="add_by_default"/>
                        </group>
                    </group>
                    <group string="Attached To" groups="base.group_multi_company,base.group_no_one">
                        <field name="company_id" placeholder="Visible to all" groups="base.group_multi_company" options="{'no_create': True}" class="oe_inline"/>
                        <field name="form_field_ids" groups="base.group_no_one" widget="many2many_tags"/>
                    </group>
                    <group string="History" groups="base.group_no_one" invisible="not create_date">
                        <label for="create_uid" string="Creation"/>
                        <div name="creation_div">
                            <field name="create_uid" readonly="1" class="oe_inline"/> on
                            <field name="create_date" readonly="1" class="oe_inline"/>
                        </div>
                    </group>
                </sheet>
            </form>