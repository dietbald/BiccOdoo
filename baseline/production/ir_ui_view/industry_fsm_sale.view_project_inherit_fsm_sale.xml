<data><field name="sale_line_id" position="attributes">
                <attribute name="invisible">context.get('fsm_mode')</attribute>
            </field>
            <field name="partner_id" position="after">
                <field name="is_fsm" column_invisible="True"/>
            </field>
            <field name="partner_id" position="attributes">
                <attribute name="invisible">(not allow_billable and not is_fsm) or is_template</attribute>
            </field>
        </data>