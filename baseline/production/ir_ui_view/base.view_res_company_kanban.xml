<kanban>
                    <templates>
                        <t t-name="card">
                            <div t-attf-class="#{!selection_mode ? 'text-center' : ''}">
                                <i class="fa fa-building" role="img" aria-label="Enterprise" title="Enterprise"/> <field class="fw-bold fs-5" name="name"/>
                            </div>
                            <hr class="mt4 mb4"/>
                            <div class="row" t-if="!selection_mode">
                                <div t-if="record.email.value" class="col-6 text-center">
                                    <strong>Email:</strong>
                                </div>
                                <div t-if="record.phone.value" class="col-6 text-center">
                                    <strong>Phone</strong>
                                </div>
                                <div t-if="record.email.value" class="col-6 text-center">
                                    <field name="email"/>
                                </div>
                                <div t-if="record.phone.value" class="col-6 text-center o_force_ltr">
                                    <field name="phone"/>
                                </div>
                            </div>
                            <div t-else="">
                                <div t-if="record.email.value">
                                    <strong>Email:</strong>
                                    <field name="email"/>
                                </div>
                                <div t-if="record.phone.value" class="o_force_ltr">
                                    <strong>Phone:</strong>
                                    <field name="phone"/>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>