<form string="Config Wizard Steps">
                  <header>
                        <button name="action_launch" invisible="state != 'open'" string="Launch" type="object" icon="fa-cogs" class="oe_highlight" help="Launch Configuration Wizard"/>
                        <button name="action_open" invisible="state != 'done'" string="Set as Todo" type="object" icon="fa-exchange" class="oe_highlight"/>
                        <field name="state" widget="statusbar" statusbar_visible="open,done" nolabel="1" readonly="1"/>
                  </header>
                  <sheet>
                    <group col="4">
                        <field name="action_id"/>
                        <field name="sequence"/>
                    </group>
                  </sheet>
                </form>