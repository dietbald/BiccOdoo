<form string="Extra Hours Rules">
                <field name="active" invisible="1"/>
                <header>
                    <button name="action_regenerate_overtimes" class="btn" string="Regenerate overtimes" icon="fa-clock-o" type="object" groups="hr_attendance.group_hr_attendance_officer" help="Regenerate overtimes for this ruleset"/>
                </header>
                <sheet>
                    <header>
                        <div class="oe_title">
                            <label for="name" string="Ruleset Name"/>
                            <h1><field name="name" placeholder="e.g. Legal Rules"/></h1>
                        </div>
                    </header>
                    <group>
                        <group>
                            <field name="rate_combination_mode"/>
                        </group>
                        <group>
                            <field name="country_id" options="{'no_create': True, 'no_open': True}" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <group colspan="4">
                        <field name="description" colspan="4" placeholder="Provide a description or sources for this ruleset."/>
                    </group>
                    <notebook colspan="4">
                        <page name="overtime_rules" string="Overtime Rules">
                            <field name="rule_ids" readonly="not id">
                                <list>
                                    <field name="sequence" widget="handle"/>
                                    <field name="name"/>
                                    <field name="base_off"/>
                                    <field name="information_display"/>
                                    <field name="amount_rate" widget="percentage"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>