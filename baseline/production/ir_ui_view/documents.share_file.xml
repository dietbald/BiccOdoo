<t name="Share a file" t-name="documents.share_file">
        <t t-call="documents.public_page_layout">
            <t t-set="shareType" t-valuef="o_share_files"/>
            <t t-set="document_content_url" t-valuef="#{document.get_base_url()}/documents/content/#{quote(document.access_token)}"/>

            <div class="shadow-lg text-center bg-white p-3 rounded-3">
                <span class="fa-stack text-black-25 fa-3x">
                  <i class="fa fa-circle-thin fa-stack-2x"/>
                  <i class="fa fa-arrow-down fa-stack-1x"/>
                </span>
                <div class="fs-4 text-truncate">
                    <b t-esc="document.name"/>
                </div>
                <div>
                    Size: <t t-call="documents.format_file_size"/>
                </div>
                <div class="mt16 mb8 d-flex flex-column gap-2">
                    <a t-att-href="document_content_url" title="Download file" class="btn btn-primary rounded-pill">
                        <i class="fa fa-download fa-fw"/> Download file
                    </a>
                    <a t-if="document.file_extension in document.get_previewable_file_extensions()" t-att-href="document_content_url + '?download=0'" target="new" title="Preview files" class="btn btn-primary rounded-pill">
                        <i class="fa fa-eye"/> Preview file
                    </a>
                    <button t-if="document.access_via_link == 'edit' or document.user_permission == 'edit'" onclick="document.querySelector('.o_request_upload').click()" class="btn btn-secondary rounded-pill">
                        <i class="fa fa-upload"/> Replace file
                    </button>
                    <form t-if="document.access_via_link == 'edit' or document.user_permission == 'edit'" enctype="multipart/form-data" t-attf-action="/documents/upload/#{quote(document.access_token)}" method="POST">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <input name="ufile" type="file" onchange="form.submit()" class="d-none o_request_upload"/>
                    </form>
                </div>
            </div>
        </t>
    </t>