<data><div name="no_partner_email" position="before">
                <field name="epd_info" class="alert alert-info fw-bold w-100 mb-3" role="alert" invisible="not epd_info"/>
            </div>

            <field name="amount" position="after">
                <field name="invoice_amount_due" invisible="res_model != 'account.move'"/>
            </field>

            <field name="currency_id" position="after">
                <field name="open_installments" invisible="1"/>
            </field>

            <group name="payment_info" position="after">
                <group name="next_installments" string="Next Installments" invisible="not display_open_installments" class="mt-n4">
                    <field name="open_installments_preview" nolabel="1"/>
                </group>
            </group>
        </data>