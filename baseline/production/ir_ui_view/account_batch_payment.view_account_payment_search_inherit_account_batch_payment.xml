<data><!-- Filter on batch payments -->
                <filter name="outbound_filter" position="after">
                    <separator/>
                    <filter string="Batch Payments" name="batch_payments_filter" domain="[('batch_payment_id', '!=', False)]"/>
                    <filter string="Not in batch" name="batch_payments_filter" domain="[('batch_payment_id', '=', False)]"/>
                    <separator/>
                </filter>
                <!-- Group by batch payments -->
                <filter name="paymentmethodline" position="after">
                    <filter string="Batch Payments" name="batch_payments" context="{'group_by': 'batch_payment_id'}"/>
                </filter>

                <filter name="reconciled" position="after">
                    <filter string="Not reconciled" name="not_reconciled" domain="[('is_reconciled', '=', False)]"/>
                </filter>

            </data>